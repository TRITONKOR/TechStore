/**
 * The {@code Main} class serves as the entry point for the TechStore application.
 * It contains the main method that initializes and starts the application.
 *
 * @author tritonkor
 * @version 1.0
 */
public class Main {

    /**
     * The main method that initializes and starts the TechStore application.
     *
     * @param args Command-line arguments (not used in this application)
     */
    public static void main(String[] args) {
        Application.init();
    }
}